<UserControl x:Class="Commandex.WizardCommand.Core.WizardSteps.StepFinal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="FirstName" Grid.Column="0" Grid.Row="0"
                   HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <TextBlock FontWeight="Bold" Text="{Binding Person.FirstName}" 
                   Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,0,0,0"/>

        <TextBlock Text="MiddleName" Grid.Row="1" Grid.Column="0" 
                   HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <TextBlock FontWeight="Bold" Text="{Binding Person.MiddleName}"
                   Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,0,0,0"/>

        <TextBlock Text="LastName"  Grid.Row="2" Grid.Column="0"
                   HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <TextBlock FontWeight="Bold" Text="{Binding Person.LastName}" 
                   Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,0,0,0"/>
    </Grid>
</UserControl>
